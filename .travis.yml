language: rust
rust:
  - stable
matrix:
  fast_finish: true
cache: cargo
os:
  - linux
#  - osx

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - cmake
      - gcc
      - g++
      - binutils-dev
      - libiberty-dev
      - liblzo2-dev
  homebrew:
    packages:
      - curl
      - gcc
      - cmake
      - lzo
      - binutils
      - dwarf
      - kcov
    update: true

sudo: required

before_script:
  - cargo install cargo-kcov
  - cargo kcov --print-install-kcov-sh | sh

script:
  - cargo build
  - cargo test --all

after_success:
  - cargo kcov --all --verbose --coveralls
#  - for file in target/debug/borderlands2-*; do [ -x "${file}" ] || continue; mkdir -p "target/cov/$(basename $file)"; kcov --exclude-pattern=/.cargo,/usr/lib "target/cov/$(basename $file)" "$file"; done &&  bash <(curl -s https://codecov.io/bash)

